<div class="relative overflow-hidden mt-24">
  <div class="max-w-xl w-full mx-auto pt-4 pb-10 px-4 sm:px-6 md:px-8 flex justify-center">
    <div class="w-full bg-white border rounded-2xl shadow-xl p-8 flex flex-col items-center">
      <h2 class="text-center text-2xl sm:text-3xl font-bold text-gray-800 mb-4 dark:text-neutral-200">
        パスワードをお忘れですか？
      </h2>
      <p class="text-center text-gray-600 dark:text-neutral-400 mb-6 text-base">
        登録したメールアドレスを入力してください。<br>
        パスワード再設定用のメールを送信します。
      </p>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "w-full space-y-6" }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div>
          <%= f.label :email, "メールアドレス", class: "block text-sm font-medium mb-2 dark:text-white" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email",
                class: "py-3 px-4 block w-full border border-gray-300 rounded-lg text-sm focus:border-blue-500 focus:ring-blue-500 dark:bg-neutral-900 dark:border-neutral-700 dark:text-neutral-400 transition" %>
        </div>

        <div>
          <%= f.submit "パスワード再設定メールを送信",
                class: "w-full py-3 px-4 inline-flex justify-center items-center gap-x-2 text-sm font-medium rounded-lg border border-transparent bg-blue-600 text-white hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 transition disabled:opacity-50 disabled:pointer-events-none" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
